# SwiftPM + build output. release.sh regenerates build/ from Sources/.
.build/
build/
*.xcodeproj
.swiftpm/

.DS_Store
# secrets never live here — notary creds are in the keychain / ~/.config/composure
*.p8
*.env

# Windows build (windows/SampleSweep.spec + installer.iss write here; CI rebuilds it)
windows/build/
__pycache__/
*.pyc
